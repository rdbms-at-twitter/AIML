# MeCab 日本語テキスト処理サンプル

MeCab を使った日本語の形態素解析・抽出型要約のサンプルスクリプト集です。Amazon Aurora の説明文を題材に、形態素解析結果の表示、TF（単語頻度）ベースの抽出型要約、そしてその内部処理（トークン化・重みづけ）の可視化を行います。

## 動作環境

- Python 3.x
- [MeCab](https://taku910.github.io/mecab/) + Python バインディング (`mecab-python3` など)

```bash
pip install mecab-python3
# 辞書が別途必要な場合（例: UniDic）
pip install unidic-lite
```

> **辞書による素性フィールドの違いに注意**
> MeCab の辞書によって `node.feature`（カンマ区切りの素性）のフィールド構成が異なります。本スクリプトは、**読みが `feature[6]`、原形が `feature[7]`** に入る辞書構成（UniDic 系）を前提にしています。IPAdic などフィールド構成が異なる辞書では、読み・原形が入れ替わったりずれたりするため、該当インデックスを調整してください。

## ファイル一覧

| ファイル | 用途 |
|---|---|
| `wakachi_mecab.py` | 形態素解析の結果（表層形・品詞・品詞細分類・読み・原形）を罫線テーブルで表示 |
| `youyaku_mecab.py` | TF（単語頻度）ベースの抽出型要約。上位N文を抜き出す |
| `youyaku_verbose.py` | `youyaku_mecab.py` の内部処理（トークン化・重みづけ・スコア計算）を5段階のテーブルで可視化 |

---

## 1. wakachi_mecab.py — 形態素解析テーブル表示

### 実行している内容

1. `MeCab.Tagger()` で解析器を生成
2. 入力文（デフォルト: `"mecabの分かち書き"`）を `parseToNode()` で1形態素ずつ走査
3. BOS/EOS の空ノードを除外し、各形態素の `feature`（カンマ区切り素性）を分解
   - `feature[0]` = 品詞、`feature[1]` = 品詞細分類、`feature[6]` = 読み、`feature[7]` = 原形
4. 全角文字の表示幅（East Asian Width: 全角=2, 半角=1）を考慮して罫線位置を揃え、テーブル出力

入力文を変えるには、スクリプト末尾の `text = "..."` を書き換えます。

### 実行方法と出力例

```bash
python wakachi_mecab.py
```

```
┌────────────┬──────┬────────────┬────────────┬────────────┐
│ 表層形     │ 品詞 │ 品詞細分類 │ 読み       │ 原形       │
├────────────┼──────┼────────────┼────────────┼────────────┤
│ mecab      │ 名詞 │ 普通名詞   │            │ mecab      │
│ の         │ 助詞 │ 格助詞     │ ノ         │ の         │
│ 分かち書き │ 名詞 │ 普通名詞   │ ワカチガキ │ 分かち書き │
└────────────┴──────┴────────────┴────────────┴────────────┘
```

---

## 2. youyaku_mecab.py — 抽出型要約

### 実行している内容

1. 題材テキスト（Amazon Aurora の説明文・全10文）を「。」で文に分割し、空文を除外
2. `tokenize()` で各文を形態素解析し、**内容語（名詞・動詞・形容詞）の原形**だけを抽出
3. 全文書での単語出現頻度 `word_frequencies` を集計（これが「重み」）
4. 各文をスコアリング:

   ```
   文のスコア = Σ(文中の各内容語の全体頻度) ÷ 文の語数
   ```

   `÷ 語数`（正規化）で、長い文が有利になりすぎるのを防ぐ
5. スコア上位N文（デフォルト3文）を選び、**元の出現順に戻して**「。」で連結し要約を出力

要約する文数を変えるには `simple_summarize(text, num_sentences=N)` の `N` を変更します。

### 実行方法と出力例

```bash
python youyaku_mecab.py
```

```
Amazon Aurora (Aurora) は完全マネージド型のリレーショナルデータベースエンジンで、MySQL および PostgreSQL と互換性があります。MySQL と PostgreSQL との互換性のあるデータベースエンジンは、その高速分散ストレージを利用するようにカスタマイズされています。また、Aurora はデータベースのクラスター化とレプリケーションを自動化しスタンダード化します。
```

---

## 3. youyaku_verbose.py — 要約処理の可視化

### 実行している内容

`youyaku_mecab.py` と同じ TF ベース要約を行いつつ、その内部処理を5段階のテーブルで出力します。重みづけの仕組みを理解・デバッグするためのツールです。

出力される5ステップ:

1. **各文のトークン化結果** — 各文がどの内容語に分解されたか、語数とともに表示
2. **単語頻度（重み）上位20** — どの単語が重視されているか（`█` バーグラフ付き）
3. **各文のスコア計算** — 頻度合計・正規化スコア（バーグラフ付き）
4. **選定結果** — スコア上位N文を元の順序に復元して表示
5. **最終要約** — 連結した要約文

### 実行方法と出力例（抜粋）

```bash
python youyaku_verbose.py
```

**ステップ2: 単語頻度（重み）上位（抜粋）**

```
┌──────┬─────────────────────────┬──────┬─────────┐
│ 順位 │ 単語                    │ 頻度 │ グラフ  │
├──────┼─────────────────────────┼──────┼─────────┤
│ 1    │ Aurora                  │ 7    │ ███████ │
│ 2    │ データーベース-database │ 7    │ ███████ │
│ 3    │ 為る                    │ 7    │ ███████ │
│ 4    │ MySQL                   │ 5    │ █████   │
│ 5    │ PostgreSQL              │ 5    │ █████   │
└──────┴─────────────────────────┴──────┴─────────┘
```

**ステップ3: 各文のスコア計算（抜粋）**

```
┌─────┬──────┬──────────┬──────────────┐
│ 文# │ 語数 │ 頻度合計 │ 正規化スコア │
├─────┼──────┼──────────┼──────────────┤
│ 0   │ 11   │ 40       │ 3.64         │
│ 5   │ 14   │ 48       │ 3.43         │
│ 8   │ 8    │ 34       │ 4.25         │
└─────┴──────┴──────────┴──────────────┘
```

`÷ 語数` の正規化により、語数が少なく高頻度語が密な文8（スコア4.25）が最高評価になる、といった挙動が確認できます。

---

- Sample 

```
$ python youyaku_verbose.py

=== 1. 各文のトークン化結果 ===
┌─────┬────────────────────────────────────────────┬──────┬──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ 文# │ 文（先頭20字）                             │ 語数 │ トークン（内容語の原形）                                                                                                                                                             │
├─────┼────────────────────────────────────────────┼──────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 0   │ Amazon Aurora (Auror…                     │ 11   │ Amazon Aurora Aurora マネージド-managed リレーショナル-relational データーベース-database エンジン-engine MySQL PostgreSQL 互換 有る                                                 │
│ 1   │ MySQL と PostgreSQL が…                   │ 17   │ MySQL PostgreSQL ハイ-high エンド-end 商用 データーベース-database スピード-speed 信頼 オープン-open ソース-sauce データーベース-database コスト-cost 効率 合わせ持つ 居る 事 存ずる │
│ 2   │ 既存の MySQL および Postgr…               │ 14   │ 既存 MySQL PostgreSQL データーベース-database 現在 使用 為る 居る コード-code ツール-tool アプリケーション-application Aurora 使用 出来る                                            │
│ 3   │ Aurora は、同等のハードウェアで、…        │ 16   │ Aurora ハードウェア-hardware 標準 PostgreSQL 最大 6 倍 スループット-throughput 標準 MySQL 最大 6 倍 スループット-throughput 提供 為る                                                │
│ 4   │ Aurora には、高性能のストレージサ…        │ 7    │ Aurora 性能 ストレージ-storage サブ-sub システム-system 含む 居る                                                                                                                    │
│ 5   │ MySQL と PostgreSQL と…                   │ 14   │ MySQL PostgreSQL 互換 有る データーベース-database エンジン-engine 高速 分散 ストレージ-storage 利用 為る カスタマイズ-customize 為る 居る                                           │
│ 6   │ 基本ストレージは、必要に応じて自動的に拡… │ 7    │ 基本 ストレージ-storage 必要 応ずる 自動 拡張 為る                                                                                                                                   │
│ 7   │ Aurora クラスターボリュームは、最…        │ 11   │ Aurora クラスター-cluster ボリューム-volume 最大 256 tebibytes TiB サイズ-size 増やす 事 出来る                                                                                      │
│ 8   │ また、Aurora はデータベースのクラ…        │ 8    │ Aurora データーベース-database クラスター-cluster レプリケーション-replication 自動 為る スタンダード-standard 為る                                                                  │
│ 9   │ 通常、これらはデータベースの設定と管理に… │ 8    │ 通常 データーベース-database 設定 管理 伴う 困難 作業 属する                                                                                                                         │
└─────┴────────────────────────────────────────────┴──────┴──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

=== 2. 単語頻度（重み） 上位20 ===
┌──────┬─────────────────────────┬──────┬────────────────┐
│ 順位 │ 単語                    │ 頻度 │ グラフ         │
├──────┼─────────────────────────┼──────┼────────────────┤
│ 1    │ Aurora                  │ 7    │ ███████ │
│ 2    │ データーベース-database │ 7    │ ███████ │
│ 3    │ 為る                    │ 7    │ ███████ │
│ 4    │ MySQL                   │ 5    │ █████     │
│ 5    │ PostgreSQL              │ 5    │ █████     │
│ 6    │ 居る                    │ 4    │ ████       │
│ 7    │ 最大                    │ 3    │ ███         │
│ 8    │ ストレージ-storage      │ 3    │ ███         │
│ 9    │ エンジン-engine         │ 2    │ ██           │
│ 10   │ 互換                    │ 2    │ ██           │
│ 11   │ 有る                    │ 2    │ ██           │
│ 12   │ 事                      │ 2    │ ██           │
│ 13   │ 使用                    │ 2    │ ██           │
│ 14   │ 出来る                  │ 2    │ ██           │
│ 15   │ 標準                    │ 2    │ ██           │
│ 16   │ 6                       │ 2    │ ██           │
│ 17   │ 倍                      │ 2    │ ██           │
│ 18   │ スループット-throughput │ 2    │ ██           │
│ 19   │ 自動                    │ 2    │ ██           │
│ 20   │ クラスター-cluster      │ 2    │ ██           │
└──────┴─────────────────────────┴──────┴────────────────┘

=== 3. 各文のスコア計算 ===
┌─────┬────────────────────────────────────────┬──────┬──────────┬──────────────┬──────────────────────────┐
│ 文# │ 文（先頭18字）                         │ 語数 │ 頻度合計 │ 正規化スコア │ グラフ                   │
├─────┼────────────────────────────────────────┼──────┼──────────┼──────────────┼──────────────────────────┤
│ 0   │ Amazon Aurora (Aur…                   │ 11   │ 40       │ 3.64         │ ██████████     │
│ 1   │ MySQL と PostgreSQL…                  │ 17   │ 41       │ 2.41         │ ███████           │
│ 2   │ 既存の MySQL および Post…             │ 14   │ 46       │ 3.29         │ █████████       │
│ 3   │ Aurora は、同等のハードウェア…        │ 16   │ 48       │ 3.00         │ █████████       │
│ 4   │ Aurora には、高性能のストレー…        │ 7    │ 18       │ 2.57         │ ███████           │
│ 5   │ MySQL と PostgreSQL…                  │ 14   │ 48       │ 3.43         │ ██████████     │
│ 6   │ 基本ストレージは、必要に応じて自動的… │ 7    │ 16       │ 2.29         │ ██████             │
│ 7   │ Aurora クラスターボリュームは…        │ 11   │ 22       │ 2.00         │ ██████             │
│ 8   │ また、Aurora はデータベースの…        │ 8    │ 34       │ 4.25         │ ████████████ │
│ 9   │ 通常、これらはデータベースの設定と管… │ 8    │ 14       │ 1.75         │ █████               │
└─────┴────────────────────────────────────────┴──────┴──────────┴──────────────┴──────────────────────────┘

=== 4. 選定結果（スコア上位3文 → 元順序で復元） ===
┌─────┬────────┬─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ 文# │ スコア │ 採用された文                                                                                                                │
├─────┼────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 0   │ 3.64   │ Amazon Aurora (Aurora) は完全マネージド型のリレーショナルデータベースエンジンで、MySQL および PostgreSQL と互換性があります │
│ 5   │ 3.43   │ MySQL と PostgreSQL との互換性のあるデータベースエンジンは、その高速分散ストレージを利用するようにカスタマイズされています  │
│ 8   │ 4.25   │ また、Aurora はデータベースのクラスター化とレプリケーションを自動化しスタンダード化します                                   │
└─────┴────────┴─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

=== 5. 最終要約 ===
Amazon Aurora (Aurora) は完全マネージド型のリレーショナルデータベースエンジンで、MySQL および PostgreSQL と互換性があります。MySQL と PostgreSQL との互換性のあるデータベースエンジンは、その高速分散ストレージを利用するようにカスタマイズされています。また、Aurora はデータベースのクラスター化とレプリケーションを自動化しスタンダード化します。

```


## 題材テキスト

`youyaku_mecab.py` と `youyaku_verbose.py` は、Amazon Aurora の説明文（全10文）を題材に固定で埋め込んでいます。

## 既知の改善点

現在の要約アルゴリズムは単純な TF（単語頻度）ベースのため、以下の弱点があります（`youyaku_verbose.py` のステップ2で確認できます）:

- 「為る（する）」「居る（いる）」「事（こと）」「有る（ある）」などの機能語が高頻度で上位に来てノイズになる
- 文書全体で頻出するが情報量の少ない語の重みを下げられない

改善の方向性:

- **ストップワード除外** — 機能語をスコアリング対象から弾く（最も効果的で実装が簡単）
- **品詞の絞り込み** — 固有名詞・一般名詞中心にし、補助動詞・非自立語を除外
- **TF-IDF 化** — 文書共通の頻出語の重みを下げる
- **TextRank** — 文間類似度グラフに PageRank を適用して重要文を選ぶ

## ライセンス

社内利用向けサンプル。

